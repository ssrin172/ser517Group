import Foundation

/// A basic settings class for your beacon manager.
/// Adjust the properties as needed for your project.
class Settings {
    /// Indicates whether directional features are enabled.
    var isDirectionEnable: Bool {
        // Return the default value as needed for your application.
        // For example, return true if you expect to use the directional features.
        return true
    }
}
